# ❤️ Pulsing Heart Animation

Uma animação de coração pulsante criada com HTML puro e CSS3, explorando efeitos de escala e temporização infinita.
Um projeto minimalista que utiliza **CSS Keyframes** para criar um efeito de pulsação realista em um elemento Emoji, simulando um batimento cardíaco.

## 🚀 Destaques Técnicos
- **CSS Animations:** Uso de `@keyframes` com a propriedade `transform: scale()` para garantir uma animação suave.
- **Responsividade:** O uso de unidades `vh` e `em` permite que o coração e o texto se ajustem ao tamanho da tela.
- **Zero Dependências:** Construído apenas com tecnologias web nativas.

## 🛠️ Tecnologias
- HTML5
- CSS3 (Animations & Flexbox)
